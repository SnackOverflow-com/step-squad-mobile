/**
 * A simple re-export of React Native's useColorScheme hook.
 * Used by our useThemeContext hook to detect and respond to
 * system theme changes.
 */
export { useColorScheme } from "react-native";
